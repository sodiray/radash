---
title: repeat
description: Convert a string to n times.
group: string
---

## Basic usage

Given a string return the string with the given number of times.

```ts
import { repeat } from 'radash'

repeat('hello', 3) // => 'hellohellohello'
repeat('hello', 0) // => ''
repeat('hello', -1) // => ''
```
